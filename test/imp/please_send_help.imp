.text
  # Les langages plus haut niveau peuvent inclure des instructions de tout type dans les en-têtes de boucle for.
  # Il faut donc accepter ce genre d'horreur.
  for (i := 0,
       print(97),
       if (i >= 0)
        print(10);;

       i < 10; 

       print(i + 48), 
       if (i % 2 == 0) 
         if (lol) {
           lol := false; 
           i++;
         } else 
           lol := true;
       else 
        i++;,
       print(10)) 
  {}
  print(97);
  print(10);
  exit;

.data
  i: 0
  lol: false
